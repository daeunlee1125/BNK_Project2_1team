<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.api.backend.mapper.MobileCustInfo" >
    <select id="countingDeviceId">
        SELECT COUNT (*) FROM TB_CUST_INFO
        WHERE CUST_DEVICE_ID = #{deviceId}
    </select>

    <select id="selectDeviceIdByCustInfo">
        SELECT CUST_DEVICE_ID FROM TB_CUST_INFO
        WHERE CUST_ID = #{custId}
    </select>

    <update id="updateDeviceIdByCustInfo">
        UPDATE TB_CUST_INFO SET CUST_DEVICE_ID = #{deviceId}
        WHERE CUST_ID = #{custId}
    </update>

    <select id="selectUserIdByCustInfo">
        SELECT * FROM TB_CUST_INFO
        WHERE CUST_ID = #{custId}
    </select>

</mapper>